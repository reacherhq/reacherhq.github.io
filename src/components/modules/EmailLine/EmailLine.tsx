import React from 'react';

export type EmailLineProps = React.HTMLAttributes<HTMLImageElement>;

export function EmailLine(_props: EmailLineProps): React.ReactElement | null {
  // TODO Wait for new Email Line image
  return null;
}
